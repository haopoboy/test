name: CI and CD
on:
  pull_request:
    branches: [ main, dev ]
  push:
    branches: [ main, dev ]

jobs:
  hello:
    runs-on: ubuntu-latest
    name: A job to write outputs
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Write outputs
        id: more-outputs
        uses: ./
        with:
          who-to-greet: 'Mona the Octocat'
      # Use the output from the `hello` step
      - name: Get the output time
        run: |
          echo "The time was ${{ steps.hello.more-outputs.time }}"
          echo "The docker tags were ${{ steps.hello.more-outputs.docker-tags }}"

